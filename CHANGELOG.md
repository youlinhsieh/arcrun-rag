# Arcrun 雲端引擎（零件包）版本說明

> 🔴 **這份是出貨用的原稿，不是給使用者讀的頁面。**
> 使用者要看版本紀錄，去 **[GitHub 版本發佈](https://github.com/youlinhsieh/arcrun-rag/releases)**
> ——那裡是對外發佈的唯一紀錄，只列他真的拿得到的版本。
>
> ── 為什麼搬到這裡（2026-08-18，leo「這個頁面刪除」，inkstone/arcrun-rag#41）──
> 本檔原本是 `docs-site/src/content/docs/help/changelog.md`，**同時是兩個東西**：
> 文件站上的一頁，以及出貨管線抽 release 內文的來源。
> leo 2026-08-09 就講過「不要同步，docs 的版本說明直接連回 github 的版本發佈」，
> 08-17 收口成三個字：**這個頁面刪除。**
>
> 頁面刪掉了，**這份原稿不能跟著刪** ——`installer/scripts/ship.mjs` 的
> `docs-changelog` 與 `release-record` 兩站靠它抽出每一版的內文，
> 沒有它，GitHub／Gitea 上的版本發佈點進去就是空白。
> ⇒ 它搬來 repo 根，與同樣住在根的 `RELEASE_LINE`（`1.4`）作伴，
>   形狀跟桌面版那條線一模一樣：`collector/CHANGELOG.md` ＋ `collector/DAEMON_LINE`。
>
> 🔴 **桌面版（Arcrun App，`v0.18.x`）的版本說明不在這裡**，它住在
> `collector/CHANGELOG.md`。兩條版本線是兩個產品，各自一份檔案、各自一條發佈線
> ——混在一個檔裡正是 D95 要拆掉的那種扭曲。
>
> ── 寫法 ──────────────────────────────────────────────────────────────
> 每一版一個 `## <版號>（<日期>）` 二級標題，底下用**使用者看得懂的白話**條列
> 「以前會怎樣、現在會怎樣」，不要寫函式名或 commit 編號。
> 這段文字會**原樣**變成版本發佈的內文（`installer/scripts/github-release.mjs`
> 的 `releaseSectionFor()`），使用者點進去看到的就是它。
> 少寫一版 ⇒ 出貨線當場中止（不是安靜跳過）。

## 1.4.55（2026-08-26）

- 🔴 **問 AI 的時候，答得出來的那一段會被排到前面**：以前你問「某某跟某某發生了什麼事」，系統回給 AI 的常常是**檔名、出處、標籤**這幾塊——它們字面上跟你的問題很像，但**一個事實都沒有**。真正寫著答案的那段摘要反而被擠掉。於是 AI 只好照著檔名往下猜，**看起來像在回答，其實是在推測**。現在有事實的段落（摘要、重點、實體）會排在前面，而且系統認定某份文件相關時，會把那份文件裡有內容的段落一起帶回來。
- **你自己寫的筆記不受影響**：判斷「這塊有沒有事實」認的是系統自己產生知識卡時的固定結構，不是猜的。你手寫的東西一律當成有事實，不會被壓下去。
- **桌面小幫手同步更新到 0.18.37**：雲端被清空或重裝過的檔案，小幫手現在會自己發現並補送（詳見桌面版的版本說明）。

## 1.4.54 修正版（2026-08-26，安裝器 `installer_sha d12b493e`）

> ⚠️ **這一則沒有自己的版本號，因為版本號看不見它。**
> 版本是從雲端零件包那 23 顆算的，而這次改的是**安裝器**——它不在那個計算裡。
> 要分辨你拿到的是不是這一版，只能看安裝頁回報的 `installer_sha`。
> 🔴 **這本身是缺陷**（`inkstone/Arcrun#159`）：leo 的驗收介面是版本號，
> 而它對這次的修法是瞎的。這一則就是那個缺陷的第一個受害者。

- 🔴 **已經更新過的人，再更新一次不會再卡在「建立資料表結構」**：上一版把「更新會把每一次該做的結構調整都補上」修好了，但漏了另一半——**已經做過那些調整的人再更新一次，系統會當場停下來說「欄位重複」**，畫面停在紅點、後面幾步都沒跑。現在它認得出「這一項你早就做過了」，跳過繼續往下走，並在完成時告訴你跳過了幾項。
- **真正的錯誤仍然會停下來**：這次放行的只有「這一步先前已經做過」這一種，其他任何問題照樣會擋住並顯示原因——不會為了讓畫面變綠而把錯誤吞掉。

## 1.4.54（2026-08-25）

- 🔴 **從舊版更新上來的人，資料庫終於會一起跟上**：這是一個嚴重的問題，我們花了一整個下午把它挖出來。以前安裝或更新的時候，只會幫你的資料庫做**最早那兩次**的結構調整——後面五次從來沒有被送出去過。結果是：程式一路更新到最新，**你的資料庫卻停在好幾個月前的樣子**。平常看不出來，直到某一天你打開它，畫面說「服務暫時不可用」，登不進去，AI 也讀不到你任何一則筆記——而系統的自我檢查還是回報「一切正常」。現在安裝與更新會把**每一次**該做的結構調整都補上，舊資料也會自動搬到新的存放方式，你不需要做任何事。
- **如果你正遇到登不進去**：更新到這一版之後再開一次就會恢復，你的東西一則都不會少。

## 1.4.53（2026-08-24）

- 🔴 **App 現在預設就長得像 Arcrun**：以前一個 App 只要自己寫了配色，打開它就是另一個世界——按鈕是藍的、底是黑的、字體和寬度都跟系統對不上。現在 App 什麼都不必做，就自動跟著你選的淺色／深色、跟著整體的配色與字體走；你按「切深色」它也一起變。想自己畫的 App 仍然可以自己畫，只是要主動說一聲。
- 🔴 **App 拿到的是整個右側，不再是白底裡的一個小黑框**：以前 App 被塞在一個內縮的方框裡，四周留著系統的底色，看起來像貼上去的。現在它佔滿整個內容區——而且不管它用哪一種樣式，這塊地方多大都是系統說了算。
- 🔴 **每一頁都回得去**：以前只有 App 頁有「← 返回」，從首頁點進搜尋、知識總圖、卡片那些頁面之後就沒有出口了。現在只要不是首頁，左上角第一個位置一律是同一顆「← 返回」。

## 1.4.52（2026-08-24）

- 🔴 **手機或窄視窗打開，頁首不再擠成一行一個字**：以前把視窗縮窄（或直接用手機看），每一頁最上面那行標題會把右邊的說明文字壓成細細一直條、一行只塞得下一個中文字，而且整塊還會凸出畫面右緣、得左右拖才看得完。啟動器頁最嚴重，因為它顯示的是「晚安，＜你的名字＞」——名字越長壞得越厲害。現在擠不下時說明文字會整段換到第二行，名字再長也只是換行，不會頂破畫面。

## 1.4.51（2026-08-24）

- 🔴 **點開一個 App 之後，回得去了**：以前點進去就沒有任何按鈕能回到 App 界面，只能重新整理整頁。現在最上面有一顆「← 返回」。
- 🔴 **App 不會再把整個畫面弄壞**：以前某個 App 的配色只要寫得比較強勢，就會蓋掉整個知識庫的介面——畫面全黑、側邊欄按得到卻看不見，而且切回其他頁面還是黑的，得重新整理才救得回來。現在每個 App 被關在自己的盒子裡，它自己的樣式出不來，同時仍然自動沿用整體的配色與按鈕樣式，所以看起來還是一整套。
- **一次拿得到超過 100 筆資料**：以前不管你的知識庫裡有幾千筆，一次查詢最多只回 100 筆，而且它不會告訴你被截斷了——你看到 100 筆會以為那就是全部。現在會一併告訴你「總共有幾筆」，看得出自己拿到的是 100/4432 還是 100/100。
- **移除資料夾時，可以順便把我們放進去的隱藏檔案一起清掉**：只刪我們自己建的，你的檔案一個都不會碰；不勾那個框的話，行為跟以前一模一樣，硬碟上一個檔都不會少。
- **桌面小幫手也有 App 界面了**（0.18.36）：一打開就是九宮格，點一下就用得到，不必再開瀏覽器。

## 1.4.50（2026-08-24）

- 🔴 **左邊的選單變成四個分頁**：「App 界面」放你已經裝好、每天在用的東西；「App 市集」是還沒裝的；「AR 設定」是這個知識庫本身的設定；「App 開發」給想自己做一個的人。以前所有東西擠在同一層清單裡，越用越長，找不到自己要的那一個。
- **知識庫可以「裝 App」了**：一個 App 就是一組做好的功能加上它自己的畫面，裝上去左邊就多一格，不用的時候可以卸載，卸載不會刪掉你已經存進去的資料。這一版先把裝與卸的路打通，市集裡的東西會陸續補上。
- **App 的圖示改成線條風格**，跟整個介面的視覺一致，不再是一堆彩色方塊。
- **修好 AI 助理連接埠的一個舊參數**：以前工具說明裡還留著一個早就不用的「api_key」欄位，會讓接上來的 AI 誤以為要填金鑰。現在拿掉了。

## 1.4.49（2026-08-18）

- 🔴 **AI 回答你的時候，會把「它怎麼找到答案的」畫成一棵樹**：從你問的問題 → 它從知識圖譜挑中哪幾個子庫 → 每個子庫讀進了哪些內容 → 最後指到原文的哪一段。線條是資料夾樹那種直角折線、單色，一眼看得懂層次；點節點上的圓圈可以把整支收合起來。以前只給你一段文字答案，你沒辦法判斷它是真的讀到東西還是編的。
- 🔴 **出處會告訴你原稿放在哪一台電腦上**：如果你有兩台電腦、或再加一台 NAS，同一個檔名在不同機器上以前會被當成同一份，出處只顯示其中一台。現在每一台都看得見，你知道要去哪裡找原稿——而知識本身仍然統一在一個庫裡。
- **舊資料完全不受影響**：先前收進來的內容沒有機器資訊，那一格會誠實地空著（顯示「未知來源」），系統不會替你猜一個機器名。


## 1.4.48（2026-08-18）

**建議更新**——這版讓 AI 問答**先找對地方再回答**，而且看得到它讀了什麼。

- 🔑 **問答會先挑對子庫再讀原文**：以前是把整個知識庫拿去比對相似度，題目一冷門就容易抓到不相干的段落。現在會先用關聯（誰跟誰有關）挑出真正相關的那幾個子庫，再進去讀原文——冷門題目也答得準。
- 👀 **看得到它是怎麼找到的**：問答畫面會顯示這次挑了哪些子庫、讀了哪幾份原文，答案不再是黑箱。
- 🗺️ **藏書地圖不再列出你其實沒有的東西**：先前地圖會列到已經不存在或不屬於這個庫的內容，看起來像資料還在。現在地圖只反映真的有的。
- 🔍 **「排除某類內容」真的會生效**：先前這個設定在某些查詢路徑上會被忽略，看起來像沒作用。
- 📄 **要原文就給原文**：先前查詢結果的前段常被地圖摘要塞滿，真正的原文段落被擠掉。

## 1.4.47（2026-08-15）

**建議更新**——這版讓知識庫「該有的零件」一次裝齊。

- 🔑 **裝好就是完整的**：先前用安裝頁裝出來的知識庫**只裝了一部分零件**，缺的那些沒有任何機制會補上——結果就是有些功能（例如關聯圖、部分查詢）根本不會動，而畫面上看不出少了什麼。這版一次把該有的都裝上。
- 這版**不影響你已經同步的資料**，只補齊程式。

## 1.4.46（2026-08-15）

**建議更新**——這版修好「裝好了卻什麼都問不到」。

- 🔑 **問答與同步恢復正常**：先前用安裝頁裝好的知識庫，**問任何問題都會失敗、檔案也同步不進去**——畫面上只看得到一串錯誤代碼，看起來像網路有問題，其實不是。現在裝好就能直接問、直接同步。
- 這個問題只影響**用安裝頁裝出來的知識庫**；如果你的一直都能用，這版對你沒有差別。

## 1.4.45（2026-08-14）

<!-- 2026-08-14 補：這版把 1.4.44 之後累積的兩件事一起出貨——08-13 的 Logseq 子資料夾
     保護（原標在這個版號下）＋ D87／Arcrun#119 全新安裝修復。stage 已驗證通過（瀏覽器
     實走建帳號流程成功），還沒推 prod（需 leo D20 開閘）。 -->

- 🔑 **全新安裝現在建得起第一個帳號了**：先前全新裝好的實例，第一次設定管理員帳號密碼時會卡住（系統以為「還沒準備好」），必須額外手動操作才能繼續。現在裝好就能直接設定帳號、馬上登入，不用再多做任何事。
- 🛡️ **同步小幫手監看子資料夾時，你的筆記庫保護不會再失效**：如果你監看的是筆記庫底下的某個子資料夾（例如 `KB/docs`），先前的保護會誤判成「這不是筆記庫」，整理結果照樣被 Logseq／Obsidian 當成你自己寫的新頁面收編。現在不管你監看哪一層都認得出來。
- 🏷️ **機器寫的檔案一眼認得出來**：整理產物一律帶固定前綴，打開資料夾就分得清哪些是你自己寫的、哪些是小幫手整理的。
- 🔁 **上次裝到一半失敗，現在可以直接再裝一次**：安裝中途斷掉（關掉視窗、網路不穩）會在你的 Cloudflare 帳號留下半套資源，再裝一次就被擋下說「名字已經有人用了」，只能自己進後台一個個刪。現在會認出那些就是你上次裝的，直接接手裝完；帳號上東西很多時也會看完整份清單，不會只看第一頁而漏掉。

## 1.4.44（2026-08-13）

- 🔍 **藏書地圖不再把「還沒整理」講成「沒有東西」**：先前地圖只顯示「萃出幾條關係」，一個庫如果內容都在、只是還沒被整理過，就會顯示 0——看起來像空的。現在會分開告訴你「有幾筆內容」和「萃出幾條關係」，AI 也不會再因此以為你的知識庫是空的而放棄查詢。
- 🗣️ **AI 說「查不到」時不會再說成「沒有這個東西」**：先前遇到權限或連線問題時，畫面上會顯示「不存在」——那會讓人（和 AI）以為東西真的沒有，往完全錯的方向找。現在會誠實講「讀不到」，並說明下一步怎麼辦。
- 🚚 **修好的東西不會再靜默地沒送出去**：出貨流程先前只檢查「知識庫零件有沒有變」，安裝頁自己改了它看不到，於是回報「完成」卻什麼都沒更新。現在它也會看安裝頁本身。

## 1.4.43（2026-08-13）

建議更新——這一版在修**你的知識庫看起來像是空的**。

- 🗺️ **藏書地圖不會再說你什麼都沒有**：
  打開「總圖」時，系統是拿一組**寫在設定裡的預設名字**去比對你的資料屬於誰——
  而那個名字跟你資料實際登記的名字**可能對不上**（例如資料搬過家、改過名之後）。
  對不上的結果不是報錯，是**安靜地一筆都不顯示**：你有一千多條關聯，畫面上卻是「0 個庫」。
  現在改成**跟你存東西時用的是同一個身分**，存得進去的就一定看得到。
  - 搜尋與問答本來就正常，這次修的是**地圖那一格**。
  - 🔴 如果哪天真的讀不出你的身分，它會**明講「讀不到」**，不會再拿一個預設值頂替
    ——「讀不到」和「你沒有」是完全不同的兩件事，不該長得一樣。

- 🔒 **順帶把這個坑封起來**：
  同一個寫法在前一天才害過一次（AI 助理查不到任何東西）。這次除了修好，
  還加了一道檢查：**以後有人再寫出同樣的寫法，程式就編譯不出來。**
  不是靠人記得，是機器擋著。

## 1.4.42（2026-08-12）

建議更新——這一版在修**更新這件事本身**。

- 🏷️ **更新完之後，設定頁還看得到版本號**：
  以前用指令更新完，設定頁的「版本」會變成「無法讀取目前版本」——
  因為重新部署會把版本標籤連同你的其他設定一起洗掉。
  現在**版本標籤每次更新都重新烙上，你自己調過的設定則原封保留**。
  - 這件事比看起來重要：版本號是你判斷「我到底更新到了沒」的唯一依據，
    而小幫手也靠它判斷雲端是不是太舊。它消失時，小幫手會一直以為你需要更新。

- 🗄️ **更新不會再報「部署物缺某某檔案」**：
  有兩個資料庫升級檔**從來沒被打包進來過**——它們被一條「不要收資料庫備份」的規則
  誤判成備份擋掉了。於是每個人更新到最後都會看到兩行紅字。
  現在它們正常隨版本出貨，更新可以乾淨跑完。

- 🔒 **順帶把 credential 目錄搬到正規位置**：
  上面那兩個升級檔做的事——把「你有哪些金鑰」這份目錄從一張多餘的表搬進標準結構。
  **你的金鑰本身不在那張表裡**（它們在 Cloudflare 的保險箱），所以這次搬移不會動到任何金鑰。

## 1.4.41（2026-08-12）

建議更新——這一版在修**你的 AI 助理用什麼身分看你的資料**。

- 🔐 **AI 助理現在用「你」的身分查東西**：
  你把 MCP 掛上去的時候輸入過帳號密碼，那跟你登入 Portal 是同一件事——
  **那個動作本身就是授權**。但以前系統沒有把你的身分接住，它驗完只記得「密碼對不對」，
  轉頭改用一把系統內部的鑰匙去查資料。那把鑰匙在部分實例上根本沒配對成功，
  於是助理問什麼都回「未授權」。
  現在它帶著你的登入身分去查，**你看得到的它就看得到，你看不到的它也看不到**。
  - ⚠️ **更新後要重新連一次 MCP**：舊的連線沒有身分資訊，系統會直接請你重連，
    而不是偷偷退回舊的那條路。

- 🛡️ **別人不能靠編號讀到你的東西**：
  以前只要知道一筆資料的編號就讀得到它，系統不會檢查那筆是不是你的；
  而助理走的那條路也繞過了「你有權限看哪些庫」的過濾。
  現在**不是你的資料，跟不存在一樣回「查無此筆」**——不會因為錯誤訊息不同而洩漏它存在。

## 1.4.40（2026-08-12）

建議更新——這一版在修**它動到你自己的檔案**這件事。

- 📓 **它不會再蓋掉你 Logseq 裡同名的頁面**：
  以前小幫手把整理出來的卡片寫回你的筆記庫時，用的是**跟你的檔案一樣的命名方式**
  （例如 `2026_08_12.md`），於是你打開 Logseq 會看到自己的日誌被一份機器寫的內容取代。
  現在機器寫進去的每個檔案都帶 `arcrun-` 前綴，不可能再跟你的頁面撞名。
  - 已經被弄亂的舊檔案，用 `collector tidy` 收拾。**它預設只列給你看、什麼都不動**，
    確認清單沒問題後再加 `--apply`。
  - ⚠️ 這個指令**從不刪除任何東西**：只有改名與搬移。覺得搬錯了，自己搬回去就是。
    舊版小幫手鋪進去的開發用檔案會被移到 `.arcrun-rag/legacy-template/`，內容原封不動。

- 🔑 **裝完不會再叫你去 Google 申請 AI 金鑰**：
  那一步早就不需要了（金鑰改由系統這邊供應），但安裝完成清單還留著，
  等於叫每個新用戶去做一件白工。現在清單只剩「下載小幫手」一項。

- ⏱️ **工作流裡的「等一下」不再燒運算額度**：
  以前只要流程裡有等待節點，不管設定等三秒還是三十秒，都會一路撐到運算上限被系統砍掉。
  原因是等待被放在沙箱裡執行，而那個沙箱**沒有「睡覺」這個能力**，只能空轉。
  現在等待改由引擎本身處理——等三十秒和等三秒一樣不花錢。

## 1.4.38（2026-08-12）

建議更新——這一版主要在修**搜尋**。如果你曾經覺得「東西明明在庫裡，就是搜不到」，就是這個。

- 🔍 **用一句話問，終於找得到了**：
  以前搜尋是拿你打的**整串字**去比對，所以只要那幾個字在文件裡沒有剛好連在一起，
  就會回你 0 筆——而你手上的東西其實在庫裡。
  舉個真的發生過的例子：查「Gemini 逃生口」回 0 筆，但單獨查「Gemini」有八百多行。
  現在會把你打的話拆成詞分別找，再照「命中幾個詞」排序。
  ⇒ 實測（一萬八千筆真實文件、三百題）：以前只有 11 題找得回正確那一筆，
  現在 280 題把它排進前十名、209 題排第一。單獨查一個詞的結果**逐字不變**。
- 📚 **「藏書地圖」不再每看一次就偷偷寫一批紀錄**：
  以前每打開一次總覽頁，它都會判定「資料過期」而重算一次並留下一筆新紀錄
  ——那在無聲吃掉你每天的資料庫寫入額度。現在只有資料真的變了才重算。
- ⏳ **補算語意索引改成「從最新的開始、每天有上限」**：
  以前它從最舊的資料開始補，而且沒有煞車——你今天寫的東西要排到最後，
  額度還可能一次被燒光（那份額度是跟「整理筆記」共用的）。
  現在先補新的，每天用到設定的上限就停手。
- 🧩 **AI 不會再被告知「這個零件不存在」**：
  有一批零件是系統自帶的，但清單查不到它們，於是 AI 會以為缺零件而改用別的做法
  ——即使那顆零件在同一台機器上明明跑得動。現在查得到了。

## 1.4.36（2026-08-11）

建議更新——這版才讓 1.4.35 的「忘記密碼」**真的接通**（1.4.35 的說明文字寫早了）。

- 📮 **裝好／更新完，「忘記密碼」不用你做任何事就會通**：
  之前即使按了更新，這條路還是會停在「這台還沒有設定寄信服務」，因為裝機器人一直
  沒有把「寄信要找誰」這件事交代給你的服務——現在補上了，重新跑一次安裝或更新，
  這個功能就會自動接上。
- 💬 **萬一還是沒接上，訊息改講白話**：不再出現「實例」這種詞，也不再要你「聯絡管理員」
  ——如果你是自己架的，管理員就是你自己，那句話等於叫你聯絡自己。現在會直接告訴你
  該做什麼：重新跑一次安裝／更新就好，不用摸任何設定。

## 1.4.37（2026-08-11）

這一版**你不會看到畫面上有什麼不同**，但它讓「你裝到的東西」變得可以追溯。

- 🧱 **零件改成統一編譯、放在固定位置**：
  以前安裝器每次都自己重編一份零件，**同一份原始碼在不同機器上會編出不一樣的東西**——
  這代表「我們測過的那一份」和「你裝到的那一份」不保證是同一份。
  現在零件由上游統一編好放固定位置，安裝器只負責**取用**，而且每顆都記著
  「我是哪個版本的原始碼編出來的」。
  ⇒ 之後如果你遇到問題，我們查得到你手上那顆零件的確切來源。

## 1.4.36（2026-08-11）

- 📮 **忘記密碼的信真的寄得出去了**（見下方 1.4.35 的說明；這一版是它第一次真的送到使用者手上）。

## 1.4.35（2026-08-11）

建議更新——這版讓「**忘記密碼**」這條路**第一次真的走得完**。

- 📮 **忘記密碼的信真的寄得出去了**：
  在登入頁按「忘記密碼」，現在會**真的收到一封信**，裡面有一條「修改密碼」連結；
  點進去不必輸入現在的密碼，直接設新的就好。連結只能用一次、30 分鐘後失效。
  信由 `arcrun.dev` 代寄（你自己那台實例沒有寄信的能力），
  而它在寄之前會**回頭跟你那台確認「這封信真的是你請我寄的嗎」**，確認過才寄。
  信上會寫明是哪一台實例請求的——如果那不是你按的，直接忽略，你的密碼不會有任何變化。
- 🔒 **順帶說明它為什麼不會被拿去亂寄**：代寄只收「信箱＋你那台的網址＋一張票」，
  **不收現成的連結**；連結是你那台自己交出來的，而且每台實例每小時最多 20 封。

<!-- 🔴 給後人（不對用戶顯示）：1.4.33 的條目寫著「🔗 忘記密碼可以自己救回來了」，
     但那份實作（D62）當時**根本沒有併進 arcrun main**，也不在 1.4.33 的 bundle 裡
     ——`POST /portal/password/forgot` 在 prod 回 404。commit `7acddc5` 的訊息同樣宣稱
     1.4.32 帶了它。2026-08-11 查證後才發現，全文見頂層 wiki
     `status.md`「出貨訊息說了謊」段與 `Leo/arcrun-rag#73` 的留言。
     ⇒ 本版才是那件事第一次真的到用戶手上。
     ⇒ 這也正是 #73 要解的病：出貨報告只對帳「兩邊檔案一不一樣」，
        不對帳「訊息宣稱的東西在不在產物裡」。 -->

## 1.4.33（2026-08-10）

建議更新——這版修的是「**改完密碼，就被自己的系統踢出去**」。

- 🔑 **改完密碼不會再被登出**：
  以前按下「更新密碼」之後，畫面會把你踢回登入頁；而這時候瀏覽器記住的還是**舊密碼**，
  你照著填就一直「email 或密碼錯誤」，試幾次還會被鎖 15 分鐘——**看起來就像系統把你關在門外**。
  原因是密碼存好之後，Cloudflare 要花幾十秒把新設定鋪到各地，
  這段期間系統會誤判成「查無此人」，然後把你的登入狀態直接刪掉，**鋪好了也回不來**。
  現在這段期間會誠實告訴你「正在更新中，你並沒有被登出」，並且**永遠不會**因為一時讀不到就刪掉你的登入狀態。
- 🔗 **忘記密碼可以自己救回來了**：
  在登入頁按「忘記密碼」，會收到一條「修改密碼」連結；點進去**不必輸入現在的密碼**，
  直接設新的就好。連結只能用一次、30 分鐘後失效。
  （這條路要能寄信才會通，見下方「還沒到你手上的」。）
- 🧩 **改密碼與忘記密碼變成同一個機制**：
  同一個畫面、同一條寫入路徑，差別只有「要不要輸入現在的密碼」。
  以前是兩套各自實作，而兩套都會壞。
- 🔌 **新安裝的實例終於真的有 MCP**：
  以前安裝器不會裝 `arcrun-mcp`，所以照說明去 claude.ai 加連線會失敗。現在它在安裝清單裡了。
- 🩺 **一條指令就看得出這台的 MCP 是哪一版**：`/health` 會回報版本與認證方式。

**還沒到你手上的**：忘記密碼的**寄信**那一段還沒開啟——按下去目前會誠實回報「尚未設定寄信」，
不會假裝已經寄出。等寄信服務上線後會再發一版。

## 1.4.30（2026-08-10）

建議更新——這版修的是「**搬了資料就登不進去**」。

- 🔑 **換掉或搬動知識資料庫之後，你還是登得進去**：
  以前登入帳號跟知識資料存在同一個地方，所以只要換了資料庫、或系統換了一組內部代號，
  等於連鑰匙一起換掉——畫面只會跟你說「email 或密碼錯誤」，讓你以為是自己記錯密碼。
  現在登入資料有自己的家，搬資料不會動到它。
- 🚪 **登不進去時會告訴你到底發生什麼事**：
  如果這台實例根本沒有任何登入資料（不是密碼錯），畫面會直接說出來並告訴你怎麼救，
  而且這種情況**不會**把你算進「密碼錯太多次」的鎖定。
- ⏱️ **剛建好帳號就登入，不會再被自己鎖在門外**：
  以前設定完帳號馬上登入會連續失敗幾次、然後被鎖 15 分鐘。現在第一次就進得去。
- 📝 **安裝時如果這個帳號早就存在，會說實話**：
  以前你在安裝畫面填的新密碼會被安靜丟掉，實際生效的還是原本那組，而畫面不會提。
  現在會明白告訴你「你剛才輸入的密碼沒有被採用，目前的密碼仍是當初那一組」。

## 未發佈（已在測試場，還沒推正式）

- 🏠 **有多個 Cloudflare 帳號的人，安裝前會先問你要裝到哪一個**：
  以前安裝器不會問，自己挑了清單上的第一個——而那個清單的順序是不保證的。
  也就是說，同一個人、同一組 Email、不同時間安裝，**可能裝到不同的帳號**，
  而畫面照樣顯示成功、照樣給你網址；等你去自己以為的那個帳號找，什麼都沒有。
  現在：底下有兩個以上帳號會請你選（只有一個帳號的人不會被多問一句）；
  之前裝過的人回來更新，會自動裝回原本那一個；**裝完的畫面也會寫清楚裝在哪個帳號**。

## 1.4.29（2026-08-09）

- 🧹 **安裝時不再多裝 19 個用不到的零件**：測試場的安裝包裡混進了一批舊版本留下的零件，
  導致同一套產品在測試場裝出來是 24 個服務、正式環境卻是 5 個。
  用不到的那 19 個不但佔你 Cloudflare 帳號的額度，也讓「測試場驗過沒問題」這句話失去意義——
  因為驗的根本不是你之後會拿到的東西。現在兩邊裝出來完全一樣。
- 🖥️ **小幫手更新到 v0.18.25**：測試場的安裝包以前不會自動帶上最新版的桌面小幫手，
  等於你在測試場按「檢查更新」永遠拿到舊版。現在打包時會自動帶上。

## 1.4.28（2026-08-09）

- 🔑 **設定頁補上 MCP 連接網址**：說明文件一直寫「登入 portal → 設定頁可以複製 MCP 網址」，
  但畫面上其實從沒真的顯示過——照文件走一定找不到。現在設定頁看得到、複製得到。
- 🗓️ **管理頁新增「執行紀錄保留期」卡片**：可以直接在畫面上看目前保留幾天、改天數，
  企業稽核需要的話也能勾選「不刪除」。以前這件事只能靠管理員自己下指令。
- 🔓 **管理員忘記密碼不再永久卡死**：以前唯一的管理員如果忘記 portal 密碼，
  系統只會叫他「聯絡管理員」——但他自己就是管理員，沒有下一步。現在有自救的出口。
- 🔴 **語意搜尋故障時，畫面照實說是故障**：以前語意搜尋壞掉，畫面會顯示「還沒開通、
  想用請聯絡我們開通」，讓人誤以為要自己申請——但那其實是我們這邊出問題，
  不需要你做任何事。現在畫面會直接說「目前故障、我們的問題」。

---

**怎麼更新** → 見[怎麼更新版本](https://rag.arcrun.dev/docs/use/update/)
